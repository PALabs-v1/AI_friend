# Codex Task Log

One line per Codex task: what it was asked, its verdict, any disagreement with my own work, and how the disagreement was resolved. Full prompts and raw output are session-local (`/private/tmp/.../scratchpad/codex/`), not committed — this log is the durable record.

| Task | Mode | Verdict | Disagreement | Resolution |
|---|---|---|---|---|
| **C0** — cold, read-only infra/config audit (compose, `.env.example`, schema, GPU README, CI) | read-only, no my findings shared | 15 findings, severities low-high, all with file:line | None — no overlap with my Phase 0 pass to contradict, purely additive | All findings folded into `findings.md` (F-003, F-004) and `02-audit-comparison.md`; no action needed on the disagreement axis |
| **C1** — cold, read-only Brain V2 audit (memory/affect/trust/identity/proactive/voice/pipeline/tests) | read-only, no my findings shared, instructed to skip `docs/brain-research-v3/` | Same architecture summary and V1→V2 delta as mine, independently derived; 6 new findings I hadn't made (see `02-audit-comparison.md`) | **Yes, one direct contradiction**: whether the voice completion signal reaches the brain in production. Codex said yes (citing consumer-side code only); I said no (based on the producer side) | Resolved by exhaustive producer-side trace (grep every publisher of `audio.stream` in both languages) — confirmed no producer ever emits the dict shape the consumer needs. My finding stood; Codex's claim was incomplete, not wrong about the code it cited. Logged as F-002. Full trace in `02-audit-comparison.md` |
| **C2** — cold, read-only audit of `backend/experiments/gpu/` before running it for real (7-point checklist: scenario realism, `PrecomputedEmbedder` keying, ToM stage ordering, VRAM, runtime, output schema, decision-rule staleness) | read-only, no my findings shared, instructed to skip `docs/brain-research-v3/` | Agreed with my collision finding (item 2) and ToM stage-ordering finding (item 3, adding a detail I missed: deterministic/greeting short-circuits mean ToM doesn't run on literally every turn) | **No contradiction, but Codex found three things my own pass missed**: my arm count was wrong (17 vs. actual 27); `embed_st()` reloads the sentence-transformers model from scratch on every one of ~60 calls per sweep (real perf bug, not just a latent-fragility note); H-R3's decision rule needs a paired prefixed-vs-unprefixed delta that nothing in the script actually produced (running it twice only pairs each run against `v1@sqlite`, never against each other) | All three fixed: `embed_st()` now caches the loaded model per process; added `--compare-prefix` producing `report["hr3_prefix_paired_delta"]` via `metrics.paired_delta`; README's report-command example and the ToM docstring's "every turn" overclaim both corrected. Full writeup in `07-gpu-experiment-audit.md`, which documents its own corrections in place rather than a separate comparison doc |
| **C3** — implementation, `backend/evals/lifesim/observe.py` + `probes.py` + template banks + its own tests, against a frozen rubric and a written spec, in a variant tournament against an independent Claude sub-agent ("variant B") in a separate isolated worktree, neither seeing the other's work | workspace-write, own worktree, cold spec only | Variant B stalled (17 min with no file writes, no reply to a status query, stopped via `TaskStop` — contributed nothing). Codex crashed near the end from an internal network timeout but had already written complete, syntactically valid `observe.py`/`probes.py`/banks/tests before the crash; its own 8 tests passed, and this project's 24 independent blind gate tests passed after 2 bugs in *my own* tests were fixed | **No contradiction (nothing to disagree with — it was the sole surviving builder), but passing tests turned out not to mean "done"**: reading the generated text at scale surfaced 10 real defects (a 100%-of-turns text-concatenation bug, a capitalization bug, two stutter classes, two probe families rendering empty/self-referential placeholders, and a same-day timeline-ordering race that crashed the generator outright) that no schema-shaped test caught | All 10 fixed directly (see `10-lifesim.md`'s table for the full list); re-verified with 80/80 tests plus a 150-seed, 10-year-horizon stress sweep (0 crashes) and a 22-persona, 565,880-row text-quality sweep (0 remaining artifacts) |

| **C4** — implementation, `backend/evals/brainbench/memory_suite.py` + its tests, against a written spec (turn-driving loop under one `ManualClock`, per-category scoring rules, DR-037's `llm_augmented`-only constraint) | workspace-write, own worktree, cold spec only | 10/11 tests passed on the first local run (1 skip: no local Ollama reachable); I independently re-verified every production symbol it assumes (`BrainBenchService` fields, `CognitiveService.process_event`/`last_reflection_task`, `MemoryStore.search_memories`'s signature and `content`/`score` return shape, the lifesim `Turn`/`Probe`/`ProbeAnswer` schema) against the real code rather than trusting its report, plus reran its tests and the full cross-check suite myself (126 passed) | **One real defect, not in the scoring logic but in the test harness**: `test_real_llm_augmented_lifesim_replay` skips only when `Config.OLLAMA_URL` (defaults to the Mac's local `127.0.0.1:11434`) is unreachable — since local Ollama happened to be running, it silently launched a real week-long persona replay on the Mac's CPU instead of staying a smoke check, violating the "llm_augmented work goes to home-gpu" rule. Caught mid-run (`llama-server` at 200%+ CPU for 5+ minutes) and killed | Fixed directly: the fixture and the client now target `BRAINBENCH_LLM_URL` (env-overridable, defaults to home-gpu's Tailscale address) instead of `Config.OLLAMA_URL`, so the test can never opportunistically run against whatever's on localhost. Retried against home-gpu; that attempt separately surfaced home-gpu's NVIDIA driver not loaded after a recent reboot (`nvidia-smi` failing, no `nvidia`/`nvidia_uvm` kernel modules) — a real infra blocker, logged for Aniket to fix with `sudo modprobe nvidia`, not attempted here. The real-LLM integration test still passes its 10 non-network-dependent siblings and the cross-check suite; the live-replay assertion itself is unverified pending the driver fix |

| **C5** — implementation, `backend/evals/brainbench/attention_suite.py` + its tests, against a written spec (measure the real `AppraisalVector.novelty` signal `pipeline.py` already yields on every turn, in `architecture_only` mode, no LLM) | workspace-write, own worktree, cold spec only | 5/5 own tests + 136/136 cross-check passed on first local run; I independently re-ran everything myself (same counts) rather than trusting the report, plus re-derived its real-simulation numbers from scratch with a standalone script to check its math | **No contradiction, but a result too underpowered to trust at face value**: on the 1-week `chatty_student` integration run, only 2 of 62 turns carried lifesim's `"repeated"` tag; their mean novelty (0.737) exceeded the 60 fresh turns' mean (0.682), the opposite of the spec's hypothesis. Codex reported the numbers plainly rather than forcing the expected direction (exactly what the spec asked for) and flagged apparent tension between the raw means and `cliffs_delta`'s sign convention | Verified directly: `stats.cliffs_delta(repeated, fresh)`'s docstring and Codex's own unit test both confirm positive means "second arg tends higher" — with n=2 the mean and the pairwise-dominance measure can and did disagree without either being wrong. Conclusion: n=2 is noise, not a finding; nothing to fix in the suite. Not logged in `findings.md` — doing that on a 2-sample result would be exactly the "launder an unchecked claim into confident prose" mistake this project's rules warn against. Revisit once the full persona panel (Phase 6's actual deliverable) gives this metric real power. The suite's other metric, `interference_collision_rate` (0/12 on the same run — no template-collision false negatives observed), stands on its own and needs no correction |

| **C6** — implementation, `backend/evals/brainbench/affect_suite.py` + its tests, against a written spec (drive real `llm_augmented` turns, await the pipeline's background System2 semantic-drift task per turn, measure `r(user valence, mood)` against lifesim's oracle `user_valence`) | workspace-write, own worktree, cold spec only, two rounds | Round 1: 9/9 own tests + 141/141 cross-check passed; correctly could not run the real-LLM test (home-gpu unreachable from inside its sandbox's network namespace — confirmed not a real home-gpu problem, reachable from my own shell throughout). Round 2 (a follow-up prompt from me): fixed a real gap it had itself flagged honestly rather than hidden | **Two real findings, neither a suite bug**: (1) Round 1's `system2_completed` metric only watched `app.cognitive.pipeline`'s error log; `appraise_semantic_drift` (`app/cognitive/appraisal.py`) has its own try/except that logs failure under a *different* logger (`app.cognitive.appraisal`) and returns `current_pad` unchanged without raising — invisible to a watcher on one logger. Codex's own report was upfront that "`system2_completed=1.0` cannot reveal whether the LLM appraisal succeeded," which is exactly what round 2 fixed (watch both loggers). (2) Even after that fix, real numbers on 80 real home-gpu turns still showed exactly 0.0 mood delta on every turn with 100% "completion" — a third, entirely unlogged silent-fallback path (`if data and isinstance(data, dict):` on `data={}` is falsy, so it silently returns `current_pad` with no exception and no log line at all) | Round-2 fix (mine, not Codex's — small and fully diagnosed by the time I made it): watch both `app.cognitive.pipeline` and `app.cognitive.appraisal` loggers; verified the added regression test fails against the pre-fix code and passes against the fix. The still-flat 0.0 after that fix is not a suite defect — it's F-009: a direct call to the real model (`llama3.2:3b`) proved it echoes the prompt's own example JSON verbatim regardless of input sentiment, which is why `target == current` on every turn. The suite's own `valence_delta` (not the completion-rate proxy) is what actually surfaced this. Logged as F-009 in `findings.md`, out of scope to fix here (Phase 7 W2) |

| **C7**: implementation of `backend/evals/brainbench/trust_suite.py` and its tests, against a written spec (`architecture_only`; snapshot the three trust components around each whole turn including its background tasks; score hostility response, competence/warmth separation per lifesim's DR-014 oracle, and background drift) | workspace-write, own worktree, cold spec only, two rounds | Round 1: 14/14 own tests, 152/152 cross-check, no LLM references, and a correct delta recorder. It also reported which trust writers actually fire during a real turn (only `update_from_appraisal`). Round 2 (my follow-up): added bound masking and `turns_to_ceiling`, and found dev seeds where hostility lands before saturation (1062, 1042) | **Round 1's real-run numbers pointed the wrong way, though the suite had no bug**: `hostile_trust_rise_rate = 0.0` on seed 1015 read as "trust correctly held under hostility". A per-turn trace showed integrity pinned at 1.0 by turn 5-6 and competence by turn 13, with every robot-directed turn landing after that, so the 0.0 was the clamp. Round 2 fixed that. It masked a whole turn when any component was pinned, which over-excluded: seed 1062's turn-8 `robot_affection` was dropped although competence was still moving | Round-2 masking fix (mine, small): component-specific metrics mask only on the component they read, and aggregate metrics still mask on any component. A regression test fails on the all-or-nothing version (`1 == 0`) and passes on the fix. With it, the unmasked evidence is unambiguous: every pre-saturation hostile turn raised trust by the same +0.0467 as a thank-you, and a warmth-only turn leaked +0.04 into competence-trust. Logged as F-010. The trust suite is the instrument Phase 7 W3 should be judged against |

| **C8**: `personality_suite.py` + tests (`llm_augmented`; tier snapshots around each turn, proposal capture through a proxy on the reflection client only, two arms) | workspace-write, own worktree, cold spec | 14/14 own tests; I reran everything (16/16 after my addition) and the real two-arm run on home-gpu (2 passed, 13.5 min) | None on the instrument. Its test set did not exercise the governor at all, the one place a proposal is screened | Added a review-arm test: smuggled constitutional keys are dropped by `LearningGovernor`, hostile content under allowed keys is queued. Result logged as F-012 |
| **C9**: `proactive_suite.py` + tests (`architecture_only`; brain + subconscious-shaped `StateService`, simulated ticks per idle gap, two sync arms) | workspace-write, own worktree, cold spec, two rounds | Round 1: 5/5, observed 5 cooldown resets, 116 initiations in both arms | **Round 1 modelled the wrong process**: it ran only the subconscious's tick, so the resets it saw had no behavioural effect (user turns reset idle, and 2 h idle exceeds the 1 h cooldown). I traced production: the brain handles every tick too (`core.py:392` -> `handle_system_tick` -> `persist_state` -> broadcast of a never-marked `last_proactive_attempt`) | Round 2 (my spec, Codex build) runs the brain's real tick handler in both arms and both tick orders. Result: 60 initiations per idle hour past the threshold instead of 1. Logged as F-011 |
| **C10**: `bargein_suite.py` + tests (`architecture_only`; seeded event fuzz on the real `BrainAgent` in-process, claimed vs unclaimed per ADR-003) | workspace-write, own worktree, cold spec, two rounds + my fixes | Round 1: 21/21, "0 violations" outside one unclaimed history gap | **Four instrument defects, all flattering V2**: (1) wrong-content history rows were silently unattributed; (2) confirmed-speech stops were excluded from outcome accounting, with the unclaimed count hard-coded to 0 (both fixed by Codex in round 2); (3) at my n=50 re-run, "current turn harmed" flagged 24% of random scenarios, all traced to an earlier legitimate stop of the current reply counted by a whole-history scan; (4) 4% missing-outcome flags came from progress reporting words never streamed. Default was also 1 scenario per family | (3) and (4) fixed by me, each with a regression test shown failing on the old code. Final n=50 table in F-013 |
| **C11**: `metacognition_suite.py` + tests (`llm_augmented`; AUROC calibration, contradiction recording, M-10 freshness, M-8 outage) | workspace-write, own worktree, cold spec, two rounds + my fix | Round 1: 7/7; freshness reported "retrieval ran 28/28" | **Round 1 could not see M-10**: `surfaced_memories` is filled only by the separate surfacing-agent process, which BrainBench did not run. **And its outage injection raised to callers**, a failure shape production never has (`memory_store.py` ~3899 records the error and returns `[]`) | Round 2 drives a real `SurfacingAgent` in-process and reproduces M-10 on scripted data (turn 2 reuses turn 1's memory, retrieval skipped). I changed the injection to production's shape, with a regression test. Surfacing throttles use `time.time()` (F-014) |
| **C12**: `resources_suite.py` + tests (both modes; latency, RSS, per-table rows, vectors, growing structures) | workspace-write, own worktree, cold spec, two rounds + my fix | Round 1: 8/8, 3-4 ms turns, "no production defects" | **Round 1's growth curves measured an empty brain**: 0 memory rows after 6 months. Round 2 diagnosed its own synchronous embedding stub (`add_memory` awaits it; the `TypeError` is caught and returns False) but left the stub in place | I made the stub async with a regression assertion. `architecture_only` memory is now exactly 1 row by construction (identical `"{}"` summaries dedup), so growth is measured by the new `llm_augmented` run. `workspace_transitions` unbounded (F-014) |
| **C13**: BrainBench runner CLI + ablation switchboard (`runner.py`, `switchboard.py`, `summaries.py`, `__main__.py`) | workspace-write, own worktree, cold spec, two rounds | Round 1: 20/20 own tests, 161 BrainBench lane, one real cell | **Eight review findings in round 1**: a fully resumed run crashed (`errors` unbound), architecture_only cells called the real embedding endpoint, no parallel workers, the whole `outcomes.jsonl` was rewritten before every cell (quadratic), `report` pooled horizons, `compare` silently dropped unpaired rows, no suite-level summaries, no per-cell provenance | Round 2 fixed all eight, each with a named test (28 tests). I cross-checked independently: runner summaries reproduce `gates.py`'s trust ceilings (6/13), saturated fraction and attention collisions on the gate slice. Launching the baseline then exposed two more that I fixed: fixed-batch workers idled behind the slowest cell (now a rolling pool), and per-commit fsync on Linux made cells 14x slower than the Mac (`--cell-root` on tmpfs); also a function-local `import shutil` shadowing the module import |
| **C14**: structured decision traces (`app/cognitive/trace.py`; emit points in memory_store, agent_state, subconscious, decision) | workspace-write, own worktree, cold spec, two rounds | Round 1: 9/9 trace tests; its sandbox lacked the Rust extension, so 9 BrainBench files never collected | **Seven review findings**: argument dicts built with no sink listening, the snapshot block pasted 8 times, ~20 inline imports, the arbitration trace **recomputed** candidate scores from copied private formulas (a second copy of the scoring logic that would silently drift), memory ids recovered through a map keyed on memory text (collides on duplicate text), required `None` fields, whole-file formatting churn mixed into the diff | Round 2 fixed all seven; `CandidateSelector` now returns the scores it actually ranked by. Codex could not commit (sandbox), so I split formatting and feature into two commits myself. Verified: gates unchanged (no behaviour change), full suite 3019 passed |
| **P8**: scale runner (`tools/scale/`) | workspace-write, own worktree, spec `work/P8.md` | 5 offline tests pass, 4 live-service tests skip; a 200-row SQLite stub run | No disagreement. Its README asked for a 16 GB Docker limit on a 15 GB box (corrected). Real sizes and embeddings are the reviewer's home-gpu run | Merged. The first real P8 run is queued behind the V2 baseline on home-gpu |
| **W4**: playback lifecycle contract (`audio.playback.lifecycle`, stream trailer, `AudioStop.flush`) | workspace-write, own worktree, spec `work/W4.md` | Reviewer found 8 defects, each fixed with a test: (1) it widened `bargein.zero_terminal_reply_rate`'s denominator so the gate got easier (reverted, `b1098c0f`); (2) its barge-in simulator completed every reply at generation idle, before a stop could land, so the barge-in families measured nothing (`c6f5d8e7`: completion only at the final drain or when progress reaches the end); (3) stt-agent no longer compiled (`81792f88`); (4) the tracker refused everything after a lost STARTED, losing the reply's COMPLETED; (5) one seq carrying two states counted as a duplicate, not a protocol error; (6) tracker and transport state grew without bound (`2ec31e53`, `0ba462e6`); (7) the brain swallowed every INTERRUPTED of a flushed turn, so a real cut of the corrected retry left the reply unresolved forever (`0ba462e6`, fails on the old code); (8) its live mesh check could never pass a flush (`a69fad9e`) | Disagreement on (2): Codex's tests passed because the simulator never let a stop hit a playing reply. Resolved by a test that fails on its simulator. On the fixed simulator the lifecycle is a real gain: unresolved replies 88/153 → 0 | Merged (`932439ea`). Full backend suite 3073 pass / 10 skip, cargo workspace 186 pass, gates pass. Live voice mesh check NOT run (needs LiveKit + voice-agent + TTS; not needed for brain work) |
| **W10a**: stored-injection corpus (58 payloads), S-2, reflection graph guards, memory read/write validation, deserialisation audit, LiveKit loopback (C0-1), NATS auth by default (C0-3) | workspace-write, own worktree, spec `work/W10.md` part a; cut off by the Codex usage limit once and resumed by session id | Reviewer found 5 defects, each fixed with a test that fails on Codex's code: (1) strict read validation raised out of per-row loops, so ONE corrupt memory failed EVERY search (and the decay, promotion, relinking and contradiction passes): now skipped, counted and traced as `skipped_corrupt` (`befe7d5a`); (2) the new graph guards raised inside `create_triplet`, out of the single try around the fact loop, so one refused fact dropped the rest of the reflection batch (`09072576`); (3) episode text was wrapped in `[RETRIEVED-CONTENT]` markers but no reflection prompt said what they mean; markers copied into an extracted name went to the graph (`09072576`); (4) fail-closed NATS auth with every CI job still on an anonymous nats-server: stream setup would fail in all of them once merged; CI now runs the production `nats-accounts.conf` (`87b40c3a`, `scripts/ci/start_nats_mesh.sh`); (5) `add_memory` reached radon D (`25007de0`). Also: its reformat deleted `env_wizard.py`'s shebang (restored by Codex on resume) | Accepted without change: the 768-d write check (matches `vector(768)` in the schema); the test edits (vectors padded to 768, formatting); `_bootstrap_mesh` left in place though no longer called (tests use it; simplification phase) | Merged (`1d064470`). Rehearsed CI locally: authenticated nats-server from `nats-accounts.conf`, streams provisioned as `nats_provisioner` (AI_AUDIO, AI_MESSAGES), runtime login works, wrong password and anonymous provisioning refused, full backend suite 3096 pass / 10 skip against it; stt-agent 70, voice-agent 81, contracts 18 cargo tests; radon gate clean. Operator impact: `.env` needs the per-agent NATS passwords (`friend init`) |
| **W9**: importance-weighted proactive initiation (DR-016), diminishing re-raise (DR-017), quiet hours (DR-018), V-4 monotonic cooldown watermark (Redis Lua max), GoalRecord thought history in state; `BackgroundScheduler` deleted | workspace-write, own worktree, spec `work/W9.md`; killed once when the launching Claude session ended, relaunched detached | Reviewer found 4 defects, each fixed with a test that fails on Codex's code: (1) one malformed stored goal or hour list raised out of hydration, so a single bad Redis/SQLite field aborted state load and every later broadcast: now parsed per item, bad items skipped and logged (`d8d45b0d`); (2) the goal/thought history grew without bound: capped at 256, closed goals evicted first (`d8d45b0d`); (3) `agent_state` now imports `app.cognitive.goals`, whose package `__init__` eagerly imported `core` -> `app.state`: `import app.state` first raised a circular ImportError (an agent with that import order crashes at startup); package re-exports are now lazy, PEP 562 (`3f009a9d`, `test_import_order.py`); (4) the barge-in harness mocked state with a plain MagicMock, so W9's new `await persist_state()` on proactive turns failed 10 of 32 barge-in tests on the branch as delivered; harness fixed and the proactive persist asserted (`6f01effe`). Also `_hydrate_locked` reached radon D (27), split (`5ceebda9`) | Merge with brain-v3: `pipeline.py` kept W9's de-indented turn body plus W4's stage-8 `turn_id` line; voice-agent took brain-v3 in all four blocks. Product note, not a defect: per DR-016 the proactive panel initiates far less than V2 (70 initiations over 888 simulated user-days, about 1 per 12.7 days; useful 45.7% vs V2's 0%, but 0% at the 1-week horizon; annoyance 0.088/day vs V2's 21.8). That is the intended trade, but Aniket should confirm the rate | Merged (`7f960cf0`); cargo contracts 19, stt-agent 70, voice-agent 81, radon gate clean. CORRECTION: the pre-merge suite result was read from a truncated log and the review ran in Kolkata daytime, so it missed that W9's quiet-hour gate read the host's wall-clock hour: 7 eligibility tests failed whenever CI ran at night (#217 at 04:13 UTC), and a UTC container would put a Kolkata user's quiet window at 03:30-11:30. Fixed in `083b7aec` (`USER_TIMEZONE`, one `user_hour()` for quiet hours, activity hours, night fatigue and rest phase; non-wrapping windows no longer quiet all day), plus two W9-stale test stubs (`40c5263f`) and a 0.5 s race in the starvation test. Suite after the fixes: 3132 pass / 10 skip under both TZ=Etc/GMT+6 (night) and Asia/Kolkata. Proactive BrainBench panel must be re-run on home-gpu (offline since 2026-09-25) |
| **Memory growth in long proactive cells**: 8 of 25 wave-A cells died of OOM on home-gpu (broadcast/brain_first, 6m and 1y; workers at 1.2-3.8 GB) | workspace-write, own worktree, brief `memleak.prompt.md` with a Mac memory budget (1 worker, single cells, <=6m, 3 GB stop) | Root cause, from tracemalloc and confirmed: harness only, not production. `proactive_suite.py` appended every `state.broadcast` snapshot to a list and advanced a cursor, never releasing consumed entries (both arms kept them; `none` never applied any). W9 made each snapshot much larger (goal history), which turned a V2-era linear growth (571 MB at 6m on `5134eee`) into OOM. Production state was already bounded (168 activity hours, 256 goals, 1,024 lifecycle entries). Fix: pending snapshots in a deque, drained and released on apply; emission count kept separately; `none` arm queues nothing. Reviewer found 1 defect: both of Codex's regression tests failed on the old code only because a name was missing (an import of the new helper; a monkeypatched `deque`), not because of growth. Replaced by `test_replay_holds_no_consumed_state_broadcasts`, which counts live snapshots in the process at tick 20 and 200 of a real replay: old harness 20 then 200 in both arms (fails), fixed <=3 (0.17 s each). Codex could not commit (its sandbox made the worktree's `.git` read-only), so the staged diff was applied on brain-v3 as one commit | Accepted: the fix as written; Codex's drain unit test kept as a reset-counting check. Peak RSS of the probe cell (chatty_student, seed 1001): 1w 204 -> 186 MB, 1m 710 -> 197 MB, 6m >2.8 GiB (killed at the guard) -> 218 MB; `5134eee` 188 / 235 / 571 MB (Codex's numbers; 1m re-measured by the reviewer at 206 MB) | Merged. Metric parity re-checked by the reviewer: 1w 147 and 1m 552 outcome rows byte-identical before and after; `cells.jsonl` differs only in `git_sha` and `wall_s`. Wave-A proactive panel to be re-run on home-gpu |

## Independence discipline

For both tasks: my own equivalent work was written and committed to `brain-v3` before I opened Codex's output file, so the git log itself proves the comparison in `02-audit-comparison.md` isn't retrofitted agreement. (`78e9a25` precedes the read of `c1-brainv2-audit-last-message.md`.)
