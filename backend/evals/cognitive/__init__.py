"""Cognitive benchmark suite: deterministic, model-free scenario tests.

Measures mechanisms (retrieval ranking, affect dynamics) rather than model
output, so it runs anywhere the backend's Python runs, in seconds, with no
network. See `evals/cognitive/README.md`.
"""
